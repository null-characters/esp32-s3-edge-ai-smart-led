# 大模块任务规划 - 语音交互模块

> 阶段一：模块搭建 - 语音交互模块

---

## 模块概述

| 属性 | 内容 |
|------|------|
| **模块名称** | 语音交互模块 |
| **所属阶段** | 阶段一：模块搭建 |
| **预估周期** | 1 周 |
| **前置依赖** | 模块1（框架迁移）搭建完成 |
| **核心目标** | ESP-SR 集成 + TTS + AEC + VAD动态路由 |
| **单元测试** | UT2: 语音模块单测（0.5 周）|

---

## 任务分解

### 2.1 ESP-SR 基础集成

| 任务ID | 任务名称 | 优先级 | 预估工时 | 交付物 |
|--------|---------|--------|---------|--------|
| T2.1.1 | 添加 ESP-SR 依赖 | P0 | 2h | idf_component.yml |
| T2.1.2 | 配置 WakeNet 模型 | P0 | 4h | sdkconfig 配置 |
| T2.1.3 | 配置 MultiNet 模型 | P0 | 4h | 50+ 条命令词 |
| T2.1.4 | 命令处理器实现 | P0 | 6h | command_handler.c |

### 2.2 DAC + 扬声器集成

| 任务ID | 任务名称 | 优先级 | 预估工时 | 交付物 |
|--------|---------|--------|---------|--------|
| T2.2.1 | MAX98357A 驱动实现 | P0 | 4h | dac_driver.c/h |
| T2.2.2 | I2S 输出通道配置 | P0 | 3h | 独立 I2S1 通道 |

### 2.3 AEC 回声消除配置

| 任务ID | 任务名称 | 优先级 | 预估工时 | 交付物 |
|--------|---------|--------|---------|--------|
| T2.3.1 | ESP-ADF AEC 流水线配置 | P0 | 6h | aec_pipeline.c |
| T2.3.2 | Reference 信号配置 | P0 | 4h | 参考 ESP-ADF 示例 |

> ⚠️ **硬件闭环关键**：AEC 需要硬件提供一路 Reference（参考音频）信号，
> 否则设备自身语音会被麦克风录入造成误识别。

### 2.4 TTS 语音反馈

| 任务ID | 任务名称 | 优先级 | 预估工时 | 交付物 |
|--------|---------|--------|---------|--------|
| T2.4.1 | TTS 组件集成 | P1 | 4h | tts_engine.c |
| T2.4.2 | 状态查询语音回复 | P1 | 4h | query_response.c |

### 2.5 VAD 动态路由 + AFE独占架构 ⭐

| 任务ID | 任务名称 | 优先级 | 预估工时 | 交付物 |
|--------|---------|--------|---------|--------|
| T2.5.1 | VAD 检测实现 | P0 | 4h | vad_detector.c |
| T2.5.2 | 麦克风流动态路由 | P0 | 6h | audio_router.c |
| T2.5.3 | **AFE独占 + RingBuffer架构** | **P0** | **6h** | **audio_ringbuffer.c** |

```
AFE独占架构：
ESP-SR AFE 独占 I2S DMA → RingBuffer → TFLM/MultiNet 消费者
```

---

## 任务分解

### 2.1 ESP-SR 组件集成

| 任务ID | 任务名称 | 优先级 | 预估工时 | 交付物 |
|--------|---------|--------|---------|--------|
| T2.1.1 | 添加 ESP-SR 依赖 | P0 | 2h | idf_component.yml |
| T2.1.2 | 配置 WakeNet 模型 | P0 | 4h | sdkconfig 配置 |
| T2.1.3 | 配置 MultiNet 模型 | P0 | 4h | 命令词配置 |
| T2.1.4 | 音频前端 AFE 配置 | P1 | 6h | AEC/BSS/NS 参数 |

### 2.2 唤醒词模块

| 任务ID | 任务名称 | 优先级 | 预估工时 | 交付物 |
|--------|---------|--------|---------|--------|
| T2.2.1 | 唤醒词模型选择 | P0 | 2h | wakenet_model.bin |
| T2.2.2 | 唤醒词检测实现 | P0 | 6h | wake_word.c/h |
| T2.2.3 | 灵敏度调优 | P1 | 4h | 阈值参数 |
| T2.2.4 | 误唤醒抑制 | P1 | 4h | 二次确认机制 |

### 2.3 命令词模块

| 任务ID | 任务名称 | 优先级 | 预估工时 | 交付物 |
|--------|---------|--------|---------|--------|
| T2.3.1 | 命令词定义 | P0 | 4h | command_def.h |
| T2.3.2 | 命令词映射表 | P0 | 2h | command_map.c |
| T2.3.3 | 命令处理器实现 | P0 | 8h | command_handler.c/h |
| T2.3.4 | 场景命令实现 | P0 | 4h | scene_commands.c |
| T2.3.5 | 调节命令实现 | P0 | 4h | adjust_commands.c |
| T2.3.6 | 查询命令实现 | P1 | 4h | query_commands.c |

### 2.4 语音助手核心

| 任务ID | 任务名称 | 优先级 | 预估工时 | 交付物 |
|--------|---------|--------|---------|--------|
| T2.4.1 | 语音助手主循环 | P0 | 6h | voice_assistant.c/h |
| T2.4.2 | 状态机设计 | P0 | 4h | va_state_machine.c |
| T2.4.3 | 事件队列实现 | P0 | 3h | va_event_queue.c |
| T2.4.4 | 回调机制设计 | P1 | 3h | va_callbacks.h |

### 2.5 音频管道

| 任务ID | 任务名称 | 优先级 | 预估工时 | 交付物 |
|--------|---------|--------|---------|--------|
| T2.5.1 | I2S 音频采集 | P0 | 4h | audio_capture.c |
| T2.5.2 | 音频缓冲管理 | P0 | 3h | audio_buffer.c |
| T2.5.3 | 音频预处理 | P1 | 4h | audio_preprocess.c |

---

## 技术要点

### ESP-SR 架构

```
┌─────────────────────────────────────────────────────────────┐
│                      ESP-SR 架构                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐             │
│  │ 麦克风    │───→│  AFE     │───→│ WakeNet  │ 唤醒检测    │
│  │ I2S      │    │ 音频前端  │    │ 唤醒词    │             │
│  └──────────┘    └──────────┘    └────┬─────┘             │
│                                        │                    │
│                                        ↓ 唤醒后             │
│                                   ┌──────────┐             │
│                                   │ MultiNet │ 命令识别    │
│                                   │ 命令词    │             │
│                                   └────┬─────┘             │
│                                        │                    │
│                                        ↓                    │
│                                   ┌──────────┐             │
│                                   │ 命令处理  │             │
│                                   │ Handler  │             │
│                                   └──────────┘             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 命令词分类

| 类型 | 数量 | 示例 | 动作类型 |
|------|------|------|---------|
| 场景切换 | 8 | "打开专注模式" | 绝对值设置 |
| 亮度调节 | 6 | "调亮一点" | 相对调节 |
| 色温调节 | 6 | "换成暖光" | 绝对值设置 |
| 模式切换 | 4 | "切换演示模式" | 状态切换 |
| 状态查询 | 5 | "现在是什么模式" | TTS 回复 |
| 取消操作 | 3 | "恢复自动" | 状态恢复 |

### 关键配置参数

```c
// sdkconfig 关键配置
CONFIG_ESP_SR_WAKENET_MODEL="wn_xiaobaitong"
CONFIG_ESP_SR_WAKENET_THRESHOLD=0.5
CONFIG_ESP_SR_MULTINET_MODEL="mn_chinese"
CONFIG_ESP_SR_AFE_AEC_ENABLE=y
CONFIG_ESP_SR_AFE_NS_ENABLE=y
CONFIG_ESP_SR_AFE_BSS_ENABLE=n  // 单麦克风不需要
```

---

## 验收标准

| 检查项 | 验收标准 |
|--------|---------|
| 唤醒词识别率 | > 95%（安静环境） |
| 命令词识别率 | > 90%（安静环境） |
| 唤醒延迟 | < 500ms |
| 命令识别延迟 | < 300ms |
| 误唤醒率 | < 1 次/小时 |
| 内存占用 | < 600KB（语音模块） |

---

## 风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| 唤醒词识别率低 | 中 | 高 | 优化 AFE 参数、增加训练数据 |
| 命令词混淆 | 中 | 中 | 增加命令词区分度 |
| 内存不足 | 中 | 高 | 使用 PSRAM、分步加载 |
| 噪声环境性能下降 | 高 | 中 | 优化 NS 参数、增加 VAD |
| **AEC 配置失败导致回声循环** | 高 | 高 | 严格按 ESP-ADF 示例配置 Reference 信号 |
| **DAC 与麦克风 I2S 时钟冲突** | 中 | 高 | 使用独立 I2S 通道（I2S0/I2S1） |
| **麦克风流资源抢占** | 高 | 高 | VAD 动态路由：无人声→TFLM，有人声→ESP-SR |

---

## 硬件配置要点

```
┌─────────────────────────────────────────────────────────┐
│              音频硬件闭环配置                            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  I2S0 (输入)          I2S1 (输出)                       │
│  ┌─────────┐          ┌─────────┐                      │
│  │ INMP441 │          │MAX98357A│                      │
│  │ 麦克风   │          │ DAC     │                      │
│  └────┬────┘          └────┬────┘                      │
│       │                    │                            │
│       ▼                    ▼                            │
│  ┌────────────────────────────────────┐                │
│  │           AEC 回声消除              │                │
│  │   Reference ← DAC 输出信号          │                │
│  └────────────────────────────────────┘                │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

*创建日期：2026-04-27*
*版本：v2.0*
*最后更新：2026-04-28*
